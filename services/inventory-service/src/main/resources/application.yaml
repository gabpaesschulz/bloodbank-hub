server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/bloodbank
    username: blood
    password: blood
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  kafka:
    bootstrap-servers: kafka:9092
